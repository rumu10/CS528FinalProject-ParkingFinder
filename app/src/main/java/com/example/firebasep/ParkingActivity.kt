package com.example.firebasep


import android.os.Bundle
import android.widget.Button
import android.widget.EditText
import android.util.Log
import android.widget.ImageView
import android.widget.Toast
import androidx.appcompat.app.AppCompatActivity
import com.google.firebase.database.*

class ParkingActivity : AppCompatActivity() {

    private lateinit var ImageWelcome: ImageView
    private lateinit var textWelcome: EditText
    //private lateinit var btnLogin: Button

    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.activity_parking)

       // ImageWelcome = findViewById(R.id.ImageWelcome)
       // textWelcome = findViewById(R.id.textWelcome)

       // textWelcome.text = "Welcome to Parking Availability Checking Page!"

         }


    }